<section id="reasons" class="px-5 pb-28 section-feather bg-pastel-reasons min-h-[150svh] flex items-center justify-center">
  <div class="mx-auto max-w-5xl">
    <div class="text-center">
      <p class="text-xs tracking-[0.35em] uppercase opacity-70">Niečo, čo cítim</p>
      <h2 class="mt-4 text-3xl md:text-5xl font-semibold">Prečo si to práve ty</h2>
      <p class="mt-4 text-base md:text-lg opacity-80 max-w-2xl mx-auto leading-relaxed">
        Pretože s tebou je všetko ľahšie.
A tichšie. A krajšie.
      </p>
    </div>

    <div class="mt-12 grid md:grid-cols-3 gap-5 reasons-grid">
      {[
        { icon: "🌸", text: "Si môj pokoj v chaose sveta." },
        { icon: "✨", text: "Pri tebe sa mi ľahšie dýcha. A viac sa smejem." },
        { icon: "🤍", text: "S tebou viem byť úplne sám sebou." },
      ].map((item) => (
        <div class="reveal card-glass soft-shadow rounded-3xl p-7 text-center">
          <div class="text-2xl">{item.icon}</div>
          <p class="mt-4 text-base md:text-lg leading-relaxed opacity-90">
            {item.text}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  import gsap from "gsap";
  import ScrollTrigger from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  const reasons = document.getElementById("reasons");
  const cards = gsap.utils.toArray<HTMLElement>("#reasons .reveal");

  if (reasons && cards.length) {
    // sekcia: jemný nábeh (ak používaš is-on class)
    ScrollTrigger.create({
      trigger: reasons,
      start: "top 80%",
      onEnter: () => reasons.classList.add("is-on"),
    });

    // karty: elegantný stagger reveal
    gsap.fromTo(
      cards,
      { y: 22, opacity: 0, filter: "blur(6px)" },
      {
        y: 0,
        opacity: 1,
        filter: "blur(0px)",
        duration: 0.9,
        ease: "power2.out",
        stagger: 0.12,
        scrollTrigger: {
          trigger: reasons,
          start: "top 75%",
        },
      }
    );
  }
</script>
